# 모델 가중치 — 용량이 커서 절대 추적하지 않음
models/
*.gguf
*.safetensors
*.bin

# 로컬 전용 설정 / 런타임 상태
mai/config/mai.env
logs/
runtime/
*.pid

# ⚠️ 이 저장소는 PUBLIC 입니다.
# 개인 기억과 진단 리포트는 여기 올리지 않습니다.
# 2대 동기화는 별도 PRIVATE 저장소로 처리합니다 (docs/TWO-MACHINE.md 4절).
mai/memory/
reports/*.md

.DS_Store

# 파이프라인 산출물 — 녹취·전사·초안은 전부 개인 데이터
mai/inbox/
mai/data/
mai/drafts/
